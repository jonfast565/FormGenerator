<!-- Api Package is root -->
<configuration>
    <connectionStrings>
        {{#each DatabaseConnections}}
        <add name="{{@key}}" connectionString="{{this}}" />
        {{/each}}
    </connectionStrings>
</configuration>